export default class Connection {
    gridX;
    gridY;
    direction;
    constructor(gridX, gridY, direction) {
        this.gridX = gridX;
        this.gridY = gridY;
        this.direction = direction;
    }
}
