export default class BaseTypes {
    static Plain = 0; // Can have requirements?
    static Binary = 1; // Gold tint
    static Ternary = 2; // Silver tint
    static Quaternary = 3; // Bronze tint
    static Names = [
        'Plain',
        'Binary',
        'Ternary',
        'Quaternary',
    ];
}
