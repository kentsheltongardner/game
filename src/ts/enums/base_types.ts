export default class BaseTypes {
    static readonly Plain       = 0 // Can have requirements?
    static readonly Binary      = 1 // Gold tint
    static readonly Ternary     = 2 // Silver tint
    static readonly Quaternary  = 3 // Bronze tint

    static readonly Names       = [
        'Plain', 
        'Binary', 
        'Ternary', 
        'Quaternary', 
    ]
}